# IContext - Core dependencies
# Tested with Python 3.11 on macOS (Darwin 24.6.0).

# Computer vision and image processing
opencv-python>=4.8.0
numpy>=1.24.0,<2.0
pillow>=10.0.0

# Object detection and tracking
ultralytics>=8.0.0

# Person analysis (pose, hands, face mesh)
mediapipe>=0.10.0

# Environment and configuration
python-dotenv>=1.0.0

# Florence-2 vision-language model
# Note: Florence-2 requires PyTorch. Install the appropriate torch version
# for your platform. See https://pytorch.org/get-started/locally/
# Example for CPU on macOS:
#   pip install torch torchvision
transformers>=4.46.0
accelerate>=0.27.0
timm>=0.9.0
einops>=0.7.0